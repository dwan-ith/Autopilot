{
  "name": "autopilot",
  "version": "0.2.0",
  "description": "Autonomous Operator Runtime",
  "scripts": {
    "server": "python run_server.py",
    "client": "npm run dev --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
